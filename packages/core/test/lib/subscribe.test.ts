import { Subscribe } from '../../src/subscribe';

describe('Subscribe', () => {
  let sub = undefined;
  beforeEach(() => {
    sub = new Subscribe();
  });
  describe('correct relations', () => {
    // sub
    // expect();
  });
});
